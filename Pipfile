[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
# Core Framework
PyGObject = ">=3.42.0"

# Speech-to-Text
openai-whisper = ">=20231117"
torch = ">=2.0.0"
torchaudio = ">=2.0.0"

# OpenVINO (Intel NPU/GPU/CPU)
openvino = "*"
openvino-genai = "*"

# Audio Input/Output
sounddevice = ">=0.4.6"
numpy = ">=1.24.0,<2.0.0"

# Text-to-Speech
piper-tts = "*"
pyttsx3 = ">=2.90"

# LLM APIs (optional cloud fallback)
openai = ">=1.3.0"
anthropic = ">=0.7.0"

# Configuration
PyYAML = ">=6.0"
jsonschema = ">=4.19.0"

# Model downloads
huggingface_hub = "*"

# Utilities
importlib-metadata = ">=6.8.0"

[dev-packages]
black = "*"
flake8 = "*"
pytest = "*"

[requires]
python_version = "3.13"
